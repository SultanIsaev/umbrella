# api/proto

Protobuf definitions for the collector's gRPC API (stats, admin) go here,
alongside generated code under `api/proto/gen/`. Not yet implemented — see
Roadmap.md, stage 4 (REST/gRPC interfaces).
